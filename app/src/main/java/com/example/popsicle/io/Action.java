package com.example.popsicle.io;

/***
 *
 * Action interface federates all kind of Actions.
 * We  use Action to produce actions and queue them, and
 * the universe would apply the actions. Then, with a create function,
 * we can generate Actions.
 * @author Hana
 */
public interface Action {
}
